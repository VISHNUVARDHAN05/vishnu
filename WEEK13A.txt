CREATING A TABLE:

SQL> create table employ3(eid number(3),ename varchar2(50),dept varchar2(20),salary number(7));

Table created.

CREATING A TRIGGER FOR BEFORE:


CREATE OR REPLACE TRIGGER TRIG
BEFORE INSERT OR UPDATE OR DELETE ON EMPLOY3
ENABLE
DECLARE
E_EID NUMBER(3);
BEGIN
SELECT COUNT(EID) INTO E_EID FROM EMPLOY3;
DBMS_OUTPUT.PUT_LINE('COUNT BEFORE ADDING EMPLOY IS'||E_EID);
END;

TRYING TO INSERT A VALUE BASED ON BEFORE TRIGGERS ON EMPLOY3:

SQL> INSERT INTO EMPLOY3 VALUES(1,'MANDEEP','MECH',45000);
COUNT AFTER ADDING EMPLOY IS0

1 row created.

CREATING A TRIGGER FOR AFTER:

CREATE OR REPLACE TRIGGER TRIG
AFTER INSERT OR UPDATE OR DELETE ON EMPLOY3
ENABLE
DECLARE
E_EID NUMBER(3);
BEGIN
SELECT COUNT(EID) INTO E_EID FROM EMPLOY3;
DBMS_OUTPUT.PUT_LINE('COUNT AFTER ADDING EMPLOY IS'||E_EID);
END;

TRYING TO INSERT A VALUE BASED ON "AFTER" TRIGGER ON EMPLOY3:

SQL> INSERT INTO EMPLOY3 VALUES(2,'VISHNU','CSE',55000);
COUNT AFTER ADDING EMPLOY IS2

1 row created.
